using System;

namespace crossql.Attributes
{
    /// <summary>
    /// Decorate a property with this attribute to indicate that it's the Primary Key for a table.
    /// </summary>
    public class PrimaryKeyAttribute : Attribute
    {
    }
}